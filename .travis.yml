dist: trusty 
sudo: required
language: cpp
compiler: gcc

before_install:
    - sudo apt update -qq
    - sudo apt install -qq cmake

install: true

script:
    - cmake
    - make
    - ./test/unit_test
